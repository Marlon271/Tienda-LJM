import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'frontend-angular',
  webDir: 'www', // Asegúrate de que aquí dice "www"
  
};

export default config;
