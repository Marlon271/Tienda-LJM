export const environment = {
  production: true, // La propiedad 'production' se establece en 'true' para entornos de producción
  apiResrURL: 'https://vts118xs-8080.use.devtunnels.ms/', // URL base para las llamadas a la API REST en producción
};


//ste archivo suele llamarse environment.prod.ts y se especifica en el archivo de configuración de compilación de Angular para que se utilice cuando la aplicación se construye para producción. Si necesitas más ayuda o tienes alguna otra pregunta, estaré encantado de asistirte.